<ips:template parameters="$server, $form" />
<div class='ipsPageHeader ipsPadding ipsBox ipsClearfix ipsSpacer_bottom cServers_header'>
	<h1 class='ipsType_pageTitle'>Editando guía del servidor: {$server->shortname}</h1>
</div>

<div class='ipsBox ipsBox--child'>{$form|raw}</div>